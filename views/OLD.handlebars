<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="=width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
        integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="column">
    <header class="row justify-between align center">
        <a href="/">
            <h3>Teachers Assistant</h3>
        </a>
        <nav class="row align center"></nav>
        {{#if user}}
        <p>Welcome, {{user.username}}</p>

        <a href="/students" class="{{#if students}}active{{/if}}">Students</a>

        <a href="/grades" class="{{#if grades}}active{{/if}}">Grades</a>

        <a href="/logout">Log Out</a>

        {{else}}
        {{#if landing}}
        <a href="/login">Login or Register</a>
        {{/if}}

        {{#if login}}
        <a href="/register">Register</a>
        {{/if}}

        {{#if register}}
        <a href="/login">Log In</a>
        {{/if}}
        </nav>
    </header>

    <main>
        <section class="intro">
            <h1>Welcome to Teacher's Gradebook</h1>
            <p>This application helps you manage your students and grades effectively.</p>
            {{#if user}}
            <p>Use the navigation above to get started by adding students and recording grades.
            </p>
            {{else}}
            <p>Please <a href="/login">log in</a> or <a href="/register">register</a> to begin using the application.
            </p>
            {{/if}}
        </section>
    </main>

    <footer class="row justify-between">
        <p>&copy; Copyright 2024</p>
        <p>Development - Instructional Team</p>
    </footer>
</body>

</html>

</header>
</body>

</html>